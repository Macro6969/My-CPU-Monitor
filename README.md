# My-CPU-Monitor
A lightweight, real-time CPU performance monitor written in C. This tool parses the Linux /proc/stat filesystem to calculate aggregate CPU load and uses system piping to identify the top resource-consuming processes. It features a flicker-free, in-terminal dashboard updated in real-time.
